# This variable is optional.
# It allows you to specify a custom command or path for the arxiv-mcp-server.
# If left blank, the system will use the default "arxiv-mcp-server" command
# installed via pip from the git repository.
ARXIV_MCP_SERVER_COMMAND=

# This variable is optional.
# It specifies the directory where downloaded arXiv papers should be stored.
# If left blank, it defaults to a folder named "arxiv_papers" in the current directory.
ARXIV_MCP_SERVER_STORAGE_PATH=